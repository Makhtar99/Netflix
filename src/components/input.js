import './input.css'
import MovieList from './firstrow.js'

function Search () {
        return (
          <section class="form">
            <MovieList/>
            
            
          </section>
        );
}
export default Search;