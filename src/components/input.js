import './input.css'
import MovieList from './firstrow.js'

function Search () {
        return (
          <section class="back">
            <MovieList/>
            
            
          </section>
        );
}
export default Search;