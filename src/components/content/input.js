import MovieList from './firstrow.js'

function Search () {
        return (
          <section className="back">
            <MovieList/>
                       
          </section>
        );
}
export default Search;